{
  "name": "yt-downloader",
  "version": "1.0.0",
  "description": "YouTube to MP3/MP4 downloader",
  "main": "downloader-server.js",
  "scripts": {
    "start": "node downloader-server.js",
    "deploy": "git add . && git commit -m 'Deploying' && git push origin main && cls && echo Building Might Take A While..."
  },
  "dependencies": {
    "hono": "^3.0.0"
  }
}

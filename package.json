{
  "name": "yt-downloader",
  "version": "1.0.0",
  "description": "YouTube to MP3/MP4 downloader",
  "main": "downloader-server.js",
  "scripts": {
    "start": "node downloader-server.js",
    "deploy": "git add . && git commit -m 'Deploying' && git push origin main && cls"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^4.19.2",
    "yt-dlp-exec": "^1.0.2"
  }
}
